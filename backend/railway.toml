[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'uv run uvicorn src.outbound_call_service:app --host 0.0.0.0 --port ${PORT:-8000} & uv run python src/agent.py start'"
restartPolicyType = "on_failure"
